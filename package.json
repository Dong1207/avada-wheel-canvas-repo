{
  "name": "avada-wheel-canvas-repo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/react"
  ],
  "scripts": {
    "dev": "yarn workspaces run dev",
    "dev:react": "yarn workspace avada-wheel-canvas-react dev",
    "build": "yarn workspace avada-wheel-canvas build:react",
    "clean": "yarn workspaces run clean",
    "preview": "yarn workspaces run preview"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
